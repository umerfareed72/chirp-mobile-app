import {StyleSheet} from 'react-native';
import {} from '../../../../utilities';

const styles = StyleSheet.create({
  mainSection: {alignItems: 'center', justifyContent: 'center', flex: 1},
});

export default styles;
