import {StyleSheet} from 'react-native';
import {} from '../../../../utilities';

const styles = StyleSheet.create({
  mainContainer: {flex: 1, width: '100%'},
  bottomSection: {position: 'absolute', bottom: 0},
});

export default styles;
